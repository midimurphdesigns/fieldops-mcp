# Optional. When set, `bun run smoke` runs an end-to-end Claude session
# against the MCP server. When unset, `bun run smoke` runs a deterministic
# scripted client that exercises the same tools without an LLM call.
ANTHROPIC_API_KEY=
